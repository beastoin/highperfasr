apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - configmap.yaml

patches:
  - path: gpu-scheduling.yaml
  - path: storage-class.yaml
  - path: config-override.yaml
